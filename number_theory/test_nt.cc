#include <bits/stdc++.h>

#include "nt.hh"

using namespace std;
using namespace codebook;

int main(){
  cout << miller_rabin(15) << endl << miller_rabin(21) << endl << miller_rabin(3013) << endl;
}
